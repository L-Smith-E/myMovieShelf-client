# myMovieShelf-client

To successfully run the Parcel Build follow the following steps:

1. Navigate to the main directory of the project in your CLI.
2. Use the command: parcel src/index.html
3. The Server should then begin to run.
